. ../PKGBUILD

source=("git+$url.git")
sha256sums=('SKIP')

prepare() {
    cd "$pkgname"
    _prepare
}

build() {
    cd "$pkgname"
    _build
}

check() {
    cd "$pkgname"
    _check
}

package() {
    cd "$pkgname"
    _package
}
